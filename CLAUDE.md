# CLAUDE.md — Claude Code 入口

@AGENTS.md

@docs/BRIEF.md

## 本会话的固定顺序
1. 先读 AGENTS.md(作业方式)与 docs/BRIEF.md(产品规格)。
2. 写任何 View 之前读 `.claude/skills/wealthclock-design-system/SKILL.md`,并在浏览器里打开 `docs/design/*.html`。
3. 碰 `Packages/FreedomEngine` 之前读 `.claude/skills/engine-verification/SKILL.md`。
4. 每个 PR 完成前用 `.claude/skills/ios-self-verify/SKILL.md` 自证。
5. 任何"决定"之前用 `.claude/skills/council-decision/SKILL.md` 开 ADR。

## 当前里程碑
见 `docs/BRIEF.md` §8。从 M0 开始。每个里程碑结束按 AGENTS.md §8 汇报,等确认再继续。
